name: Render SARX demo media

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "tools/sarx_severance_demo.cpp"
      - "tools/sarx_humanoid_demo.cpp"
      - "tools/sarx_character_demo.cpp"
      - "tools/sarx_character_severance_demo.cpp"
      - "tools/sarx_character_head_severance_demo.cpp"
      - "tools/sarx_character_voxel_demo.cpp"
      - "tools/sarx_character_voxel_thigh_demo.cpp"
      - "src/voxel_character.cpp"
      - "include/sarx/voxel_character.hpp"
      - "src/gltf_character.cpp"
      - "include/sarx/gltf_character.hpp"
      - "src/character_render.cpp"
      - "include/sarx/character_render.hpp"
      - "src/gltf_character.cpp"
      - "include/sarx/gltf_character.hpp"
      - "src/character_render.cpp"
      - "include/sarx/character_render.hpp"
      - "assets/quaternius/**"
      - "src/humanoid.cpp"
      - "include/sarx/humanoid.hpp"
      - "src/debug_render.cpp"
      - "include/sarx/debug_render.hpp"
      - ".github/workflows/demo-media.yml"
  push:
    branches:
      - "research/**"
    paths:
      - "tools/sarx_severance_demo.cpp"
      - "tools/sarx_humanoid_demo.cpp"
      - "tools/sarx_character_demo.cpp"
      - "tools/sarx_character_severance_demo.cpp"
      - "tools/sarx_character_head_severance_demo.cpp"
      - "tools/sarx_character_voxel_demo.cpp"
      - "tools/sarx_character_voxel_thigh_demo.cpp"
      - "src/voxel_character.cpp"
      - "include/sarx/voxel_character.hpp"
      - "src/gltf_character.cpp"
      - "include/sarx/gltf_character.hpp"
      - "src/character_render.cpp"
      - "include/sarx/character_render.hpp"
      - "assets/quaternius/**"
      - "src/humanoid.cpp"
      - "include/sarx/humanoid.hpp"
      - "src/debug_render.cpp"
      - "include/sarx/debug_render.hpp"
      - ".github/workflows/demo-media.yml"

jobs:
  render-demo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Configure
        run: cmake -S . -B build -DSARX_BUILD_TESTS=OFF -DCMAKE_BUILD_TYPE=Release

      - name: Build demos
        run: |
          cmake --build build --config Release --target sarx_severance_demo
          cmake --build build --config Release --target sarx_humanoid_demo
          cmake --build build --config Release --target sarx_character_demo
          cmake --build build --config Release --target sarx_character_severance_demo
          cmake --build build --config Release --target sarx_character_head_severance_demo
          cmake --build build --config Release --target sarx_character_voxel_demo
          cmake --build build --config Release --target sarx_character_voxel_thigh_demo

      - name: Render bridge frames
        run: |
          set -o pipefail
          ./build/sarx_severance_demo \
            --output build/demo_frames \
            --frames 120 \
            --cut-frame 45 | tee build/demo.log

      - name: Render humanoid frames
        run: |
          set -o pipefail
          ./build/sarx_humanoid_demo \
            --output build/humanoid_frames \
            --frames 150 \
            --cut-frame 60 | tee build/humanoid.log

      - name: Render real skinned walk frames
        run: |
          set -o pipefail
          ./build/sarx_character_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --list-joints \
            --output build/character_frames \
            --frames 180 \
            --fps 30 | tee build/character.log

      - name: Render real skinned severance frames
        run: |
          set -o pipefail
          ./build/sarx_character_severance_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --detach-root upperarm_r \
            --output build/character_severance_frames \
            --frames 210 \
            --cut-frame 90 \
            --fps 30 \
            --require-ground-contact \
            --require-articulated-limb | tee build/character_severance.log

      - name: Render real skinned left-arm severance frames
        run: |
          set -o pipefail
          ./build/sarx_character_severance_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --detach-root upperarm_l \
            --output build/character_left_arm_frames \
            --frames 210 \
            --cut-frame 90 \
            --fps 30 \
            --require-ground-contact \
            --require-articulated-limb | tee build/character_left_arm.log

      - name: Render real skinned head severance frames
        run: |
          set -o pipefail
          ./build/sarx_character_head_severance_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --output build/character_head_frames \
            --frames 210 \
            --cut-frame 90 \
            --fps 30 \
            --require-ground-contact \
            --require-rotation | tee build/character_head.log

      - name: Render Quaternius voxel left-hand detachment frames
        run: |
          set -o pipefail
          ./build/sarx_character_voxel_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --output build/character_voxel_frames \
            --frames 210 \
            --damage-frame 75 \
            --fps 30 \
            --voxel-size 0.045 \
            --require-damage \
            --require-detachment \
            --require-ground-contact \
            --require-anatomical-isolation \
            --require-walk-viability | tee build/character_voxel.log

      - name: Render Quaternius voxel left-thigh severance frames
        run: |
          set -o pipefail
          ./build/sarx_character_voxel_thigh_demo \
            --character assets/quaternius/character.glb \
            --animations assets/quaternius/animations.glb \
            --clip Walk \
            --output build/character_voxel_thigh_frames \
            --frames 210 \
            --cut-frame 75 \
            --fps 30 \
            --voxel-size 0.045 \
            --require-damage \
            --require-detachment \
            --require-ground-contact \
            --require-walk-invalidation \
            --require-anatomical-isolation \
            --require-stable-camera \
            --require-articulated-leg | tee build/character_voxel_thigh.log

      - name: Encode MP4s
        run: |
          mkdir -p build/media
          ffmpeg -y \
            -framerate 30 \
            -i build/demo_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v04c_arm_severance.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/humanoid_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v04d_humanoid_shoulder_severance.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v05_real_character_walk.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_severance_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v05_real_character_walk_severance.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_left_arm_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v06_real_character_left_arm_severance.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_head_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v06_real_character_head_severance.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_voxel_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v08_quaternius_voxel_left_hand_detach.mp4

          ffmpeg -y \
            -framerate 30 \
            -i build/character_voxel_thigh_frames/frame_%04d.ppm \
            -c:v libx264 \
            -pix_fmt yuv420p \
            -crf 20 \
            build/media/v09_quaternius_voxel_left_thigh_cut.mp4

      - name: Encode GIF previews
        run: |
          ffmpeg -y \
            -i build/media/v04c_arm_severance.mp4 \
            -vf "fps=15,scale=720:-1:flags=lanczos,palettegen" \
            build/media/palette_bridge.png

          ffmpeg -y \
            -i build/media/v04c_arm_severance.mp4 \
            -i build/media/palette_bridge.png \
            -lavfi "fps=15,scale=720:-1:flags=lanczos[x];[x][1:v]paletteuse" \
            build/media/v04c_arm_severance.gif

          ffmpeg -y \
            -i build/media/v04d_humanoid_shoulder_severance.mp4 \
            -vf "fps=15,scale=720:-1:flags=lanczos,palettegen" \
            build/media/palette_humanoid.png

          ffmpeg -y \
            -i build/media/v04d_humanoid_shoulder_severance.mp4 \
            -i build/media/palette_humanoid.png \
            -lavfi "fps=15,scale=720:-1:flags=lanczos[x];[x][1:v]paletteuse" \
            build/media/v04d_humanoid_shoulder_severance.gif

          ffmpeg -y \
            -i build/media/v05_real_character_walk.mp4 \
            -vf "fps=15,scale=720:-1:flags=lanczos,palettegen" \
            build/media/palette_character.png

          ffmpeg -y \
            -i build/media/v05_real_character_walk.mp4 \
            -i build/media/palette_character.png \
            -lavfi "fps=15,scale=720:-1:flags=lanczos[x];[x][1:v]paletteuse" \
            build/media/v05_real_character_walk.gif

          ffmpeg -y \
            -i build/media/v05_real_character_walk_severance.mp4 \
            -vf "fps=15,scale=720:-1:flags=lanczos,palettegen" \
            build/media/palette_character_severance.png

          ffmpeg -y \
            -i build/media/v05_real_character_walk_severance.mp4 \
            -i build/media/palette_character_severance.png \
            -lavfi "fps=15,scale=720:-1:flags=lanczos[x];[x][1:v]paletteuse" \
            build/media/v05_real_character_walk_severance.gif

      - name: Record provenance
        run: |
          {
            echo "commit=${GITHUB_SHA}"
            echo "frames=120"
            echo "fps=30"
            echo "cut_frame=45"
            echo "backend=CPU Body"
            echo "scheduler=full pre-cut; adaptive wound-domain + free-island closure post-cut"
            echo
            echo "[bridge demo log]"
            cat build/demo.log
            echo
            echo "[humanoid demo log]"
            cat build/humanoid.log
            echo
            echo "[real skinned character walk log]"
            cat build/character.log
            echo
            echo "[real skinned character walk severance log]"
            cat build/character_severance.log
            echo
            echo "[real skinned left arm severance log]"
            cat build/character_left_arm.log
            echo
            echo "[real skinned head severance log]"
            cat build/character_head.log
            echo
            echo "[Quaternius voxel left-hand detachment log]"
            cat build/character_voxel.log
            echo
            echo "[Quaternius voxel left-thigh severance log]"
            cat build/character_voxel_thigh.log
          } > build/media/provenance.txt

      - name: Upload prototype media
        uses: actions/upload-artifact@v4
        with:
          name: sarx-character-integration-media
          path: |
            build/media/v04c_arm_severance.mp4
            build/media/v04c_arm_severance.gif
            build/media/v04d_humanoid_shoulder_severance.mp4
            build/media/v04d_humanoid_shoulder_severance.gif
            build/media/v05_real_character_walk.mp4
            build/media/v05_real_character_walk.gif
            build/media/v05_real_character_walk_severance.mp4
            build/media/v05_real_character_walk_severance.gif
            build/media/v06_real_character_left_arm_severance.mp4
            build/media/v06_real_character_head_severance.mp4
            build/media/v08_quaternius_voxel_left_hand_detach.mp4
            build/media/v09_quaternius_voxel_left_thigh_cut.mp4
            build/media/provenance.txt
          if-no-files-found: error
